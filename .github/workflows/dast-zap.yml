name: DAST - OWASP ZAP Full Scan (canonicalization-example)

on:
  workflow_dispatch:

jobs:
  zap_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: cyse411/canonicalization-example
        run: npm install || true

      - name: Start Canonicalization Server
        working-directory: cyse411/canonicalization-example
        run: |
          node server.js > server.log 2>&1 &
          echo "Waiting for canonicalization server on port 4000..."

          for i in {1..40}; do
            if curl -s http://localhost:4000 > /dev/null; then
              echo "Canonicalization server is UP!"
              break
            fi
            sleep 1
          done

      - name: Run OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@v0.13.0
        with:
          target: "http://localhost:4000"
          fail_action: false
          allow_issue_writing: false
          cmd_options: "-a"

      - name: Upload ZAP Scan Report
        uses: actions/upload-artifact@v4
        with:
          name: zap_scan_canonicalization
          path: report.html
